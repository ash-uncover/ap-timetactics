import React from 'react'

export const App = () => {

  // #region Hooks
  // #endregion

  // #region Callbacks
  // #endregion

  // #region Rendering
  const classes = ['ap-app']

  return (
    <div className={classes.join(' ')}>
      APP
    </div>
  )
  // #endregion
}
